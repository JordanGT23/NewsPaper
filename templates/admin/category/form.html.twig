{% extends 'base.html.twig' %}

{% block title %}Ajouter une categorie | NewsPaper{% endblock %}

{% block body %}

    {% if category is not defined %}
    <h1 class="text-center my-3">Ajouter une Categorie</h1>
    {% else %}
    <h1 class="text-center my-3">Modifier une Categorie</h1>
    {% endif %}

    <div class="row">
        <div class="col-4 mx-auto">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
